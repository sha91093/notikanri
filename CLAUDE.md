# 農地管理アプリ（のうち管理）- 作成案

## プロジェクト概要

紙台帳で農地管理をおこなっている農家さんのデジタル化を支援するWebアプリケーション。
PCとスマートフォンの両方から利用でき、高齢者でも操作しやすいシンプルでモダンなUIを目指す。

---

## 機能一覧

### 1. 農地管理マップ
- 筆ポリゴン（農地の区画データ）を地図上に表示
- 背景マップは航空写真（国土地理院タイル or Google Maps Satellite）
- 自分の農地区画をタップ/クリックで選択
- 選択した区画に対して以下を入力・表示:
  - 作物名（1作目・2作目：二毛作対応）
  - 作付面積
  - 耕作者（担当農家）
  - 備考
- 区画の色分け表示（作物種別ごとに色を変える）
- 年度ごとにデータを保持・切り替え可能

### 2. 営農スケジュールカレンダー
- 月表示・週表示・日表示の切り替え
- 農作業スケジュールの登録・修正・削除
  - 作業名（耕起、播種、施肥、防除、収穫など）
  - 対象農地（区画紐付け）
  - 担当者
  - 作業日時（終日 or 時間指定）
  - メモ
- 複数人の作業予定を色分けで表示
- スマホ向けにスワイプ操作対応

### 3. 作業日誌
- 作業日誌の登録・修正・削除
  - 作業日
  - 対象農地
  - 作業内容
  - 天気
  - 担当者
  - 写真アップロード（複数枚対応）
  - 自由記述メモ
- 一覧表示・検索・絞り込み（農地別・日付別・担当者別）
- 年度別での表示切り替え

---

## 画面構成

```
┌─────────────────────────────────┐
│         ヘッダー                 │
│  [ロゴ]  農地管理  [年度選択▼]  │
├─────────────────────────────────┤
│                                 │
│                                 │
│         メインコンテンツ         │
│                                 │
│                                 │
├─────────────────────────────────┤
│  [🗺 マップ] [📅 カレンダー] [📔 日誌] │
│         ボトムナビゲーション      │
└─────────────────────────────────┘
```

- ボトムナビゲーションバーで3機能を切り替え（PC・スマホ共通）
- PCではサイドバー表示も検討

---

## 技術スタック（案）

### フロントエンド
- **フレームワーク**: Next.js 14（App Router）
- **スタイリング**: Tailwind CSS + shadcn/ui
- **地図ライブラリ**: Leaflet.js または MapLibre GL JS
  - 筆ポリゴンデータ: 農林水産省の農地筆ポリゴン（GeoJSON形式）
  - 背景タイル: 国土地理院 写真タイル（無償・商用利用可）
- **カレンダー**: FullCalendar.js または react-big-calendar
- **状態管理**: Zustand または React Context
- **フォーム**: React Hook Form + Zod

### バックエンド
- **フレームワーク**: Next.js API Routes（または FastAPI）
- **データベース**: PostgreSQL + PostGIS（地理情報対応）
- **ORM**: Prisma
- **認証**: NextAuth.js（複数ユーザー管理）
- **ファイルストレージ**: AWS S3 または Cloudflare R2（写真アップロード）

### インフラ
- **ホスティング**: Vercel（フロントエンド）+ Supabase（DB・認証・ストレージ）
  - ※ Supabase は PostgreSQL + PostGIS + Auth + Storage をまとめて提供
- **CI/CD**: GitHub Actions

---

## データモデル（概要）

```
User（農家ユーザー）
  ├─ id, name, email, password, role

FarmGroup（農家グループ）
  ├─ id, name
  └─ members: User[]

Parcel（農地区画）
  ├─ id, name, area（面積）
  ├─ geometry（GeoJSON ポリゴン）
  └─ farmGroupId

CropPlan（作付計画）
  ├─ id, parcelId, year
  ├─ crop1（1作目作物）
  ├─ crop2（2作目作物：二毛作用）
  └─ assignedUserId

ScheduleEvent（営農スケジュール）
  ├─ id, title, parcelId
  ├─ startDate, endDate
  ├─ assignedUserId, memo
  └─ year

WorkLog（作業日誌）
  ├─ id, date, parcelId
  ├─ content, weather
  ├─ assignedUserId, memo
  ├─ photos: Photo[]
  └─ year

Photo（写真）
  ├─ id, workLogId
  └─ url（S3/R2 URL）
```

---

## UXデザイン方針

### 高齢者対応
- フォントサイズ: 最小16px、見出しは20px以上
- ボタン・タップ領域: 最小44×44px（Apple HIG 準拠）
- 色のコントラスト比: WCAG AA基準以上
- 操作ステップを極力少なく（3タップ以内で主要操作完了）
- わかりやすいアイコン＋テキストラベルの併用
- エラーメッセージは平易な日本語で表示

### レスポンシブ対応
- モバイルファースト設計
- ブレークポイント: 768px（タブレット）、1024px（PC）
- PC では地図とパネルを横並びレイアウト

---

## 年度管理・引き継ぎ機能

- 年度は農業年度（例: 2025年度 = 2025年4月〜2026年3月）
- ヘッダーの年度セレクターで表示年度を切り替え
- 「次年度に引き継ぐ」ボタンで作付計画データをコピー
  - 作物名はそのままコピー or クリアを選択可能
  - スケジュール・日誌は年度別に管理（引き継ぎ不要）

---

## 複数ユーザー管理

- 農家グループ単位で管理
- ロール:
  - **管理者**: 全データの閲覧・編集・削除、ユーザー招待
  - **一般メンバー**: 全データの閲覧・自分の作業の登録・編集
- 招待はメールアドレスで送付
- グループ内の全メンバーのスケジュール・日誌を閲覧可能

---

## 開発フェーズ案

### Phase 1（MVP）
- [ ] ユーザー認証（ログイン・ログアウト）
- [ ] 農地管理マップ（航空写真背景 + 筆ポリゴン表示）
- [ ] 農地区画の選択・作物入力（二毛作対応）
- [ ] 営農スケジュールカレンダー（CRUD）
- [ ] 作業日誌（CRUD・写真アップロード）
- [ ] ボトムナビゲーション

### Phase 2
- [ ] 複数ユーザー・グループ管理
- [ ] 年度切り替え・引き継ぎ機能
- [ ] 日誌の検索・絞り込み

### Phase 3
- [ ] 通知機能（作業予定リマインダー）
- [ ] 印刷・PDF出力
- [ ] データエクスポート（CSV）

---

## 参考データソース

- 農林水産省 農地筆ポリゴン: https://www.maff.go.jp/j/tokei/porigon/
- 国土地理院 地図タイル（写真）: https://maps.gsi.go.jp/development/ichiran.html
- e-Stat 農業関連統計
